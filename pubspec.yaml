name: keel
description: A ViewModel based architecture for Flutter
version: 0.0.7
homepage: https://github.com/arklabsnz/keel-flutter

environment:
  sdk: ">=2.8.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  rxdart: ">=0.24.0  <1.0.0"
  provider: ">=4.3.2+3  <5.0.0"

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
